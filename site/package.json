{
  "name": "muxlayer-site",
  "private": true,
  "version": "0.1.0",
  "description": "MuxLayer landing site",
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ./assets/tailwind.css --minify",
    "dev:css": "tailwindcss -i ./input.css -o ./assets/tailwind.css --watch",
    "build": "node scripts/build-seo-pages.mjs && npm run build:css"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "marked": "^18.0.5",
    "tailwindcss": "^4.3.0"
  }
}
